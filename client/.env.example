# websocket endpoint for the go server.
# leave unset for local development (defaults to ws://localhost:8080).
# in production set this to the proxied, TLS-terminated endpoint, e.g.:
# VITE_WS_URL=wss://sim.elliotkirk.com/ws
VITE_WS_URL=
